<nav class="navbar navbar-default navbar-static-top">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse"
data-target="#navbar" aria-expanded="false" aria-controls="navbar">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <%= link_to 'PharmOx', root_path, { :class=>"navbar-brand" } %>
        </div>
        <div id="navbar" class="navbar-collapse collapse right">
            <% if (!user_signed_in?) %>
                <!-- <li><%= link_to 'Inscription', new_user_registration_path %></li>
                <li><%= link_to 'Connexion', new_user_session_path %></li> -->
            <% else %>
            <ul class="nav navbar-nav navbar-right">
                <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button"
              aria-haspopup="true" aria-expanded="false">
                <%= current_user.email %>
              <span class="caret"></span></a>
              <ul class="dropdown-menu">
                <li><%= link_to 'Paramètre', edit_user_registration_path %></li>
                <% if (current_user.try(:admin?)) %>
                  <li><%= link_to 'Ajouter un compte', new_user_registration_path %></li>
                  <li><%= link_to 'Ajouter un laboratoire', new_laboratory_path %></li>
                  <li><%= link_to 'Ajouter une campagne', new_campaign_path %></li>
                  <li><%= link_to 'Ajouter une saisie', new_entry_path %></li>
                  <li><%= link_to 'Ajouter les pharmacies à la db', new_pharmacy_path %></li>
                <% end %>
                <li role="separator" class="divider"></li>
                <li><%= link_to 'Déconnexion', destroy_user_session_path, method: :delete %></li>
              </ul>
            </li>
            <% end %>

          </ul>
        </div><!--/.nav-collapse -->
      </div>
</nav>
